package controller;
import model.Board;
import java.util.ArrayList;
public class Rules {
    
    public static void modify(Board board, int currentRow, int currentCol){
        int count = Count.getCount(board, currentRow, currentCol);
        
    }
}
